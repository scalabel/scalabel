import { createStyles } from '@material-ui/core'

export const contextMenuStyles = () => createStyles({
  categoryMenu: {
    width: '100%',
    marginBottom: '10px'
  },
  attributeMenu: {
    width: '100%',
    marginBottom: '10px'
  }
})
