import { createStyles } from '@material-ui/core'

export const viewerStyles = () => createStyles({
  viewer_container: {
    display: 'block',
    height: '100%',
    position: 'absolute',
    overflow: 'hidden',
    outline: 'none',
    width: '100%'
  }
})
