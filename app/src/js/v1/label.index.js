import React from 'react';
import ReactDOM from 'react-dom';
import LabelInterface from '../components/label_layout';

ReactDOM.render(
    <LabelInterface />,
    document.getElementById('labeling-interface')
);
